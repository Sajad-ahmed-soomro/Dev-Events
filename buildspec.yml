version: 0.2

phases:
  install:
    commands:
      - curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
      - apt-get install -y nodejs
      - node -v
      - npm -v
      - npm ci

  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
